apiVersion: v1
kind: Service
metadata:
  name: donation-service
  namespace: donation-service
spec:
  selector:
    app: donation-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP